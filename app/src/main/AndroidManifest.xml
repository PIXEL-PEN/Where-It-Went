<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">

        <!-- NEW LAUNCHER: prototype screen -->
        <activity
            android:name=".MainScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The proxy AddExpense (kept, but not launcher now) -->
        <activity android:name=".AddExpenseActivity" android:exported="false"/>

        <!-- Original Add-Expense/Main hybrid screen -->
        <activity android:name=".MainActivity" android:exported="false"/>

        <!-- Other screens -->
        <activity android:name=".ViewMenuActivity" android:exported="false" />
        <activity android:name=".ViewAllActivity" android:exported="false" />
        <activity android:name=".DateWiseActivity" android:exported="false" />
        <activity android:name=".MonthWiseActivity" android:exported="false" />
        <activity android:name=".CategoryWiseActivity" android:exported="false" />
        <activity android:name=".DayDetailActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".AboutActivity" android:exported="false" />
        <activity android:name=".DistributionActivity" android:exported="false" />
        <activity android:name=".TutorialActivity" android:exported="false"/>
        <activity android:name=".MainLegacyActivity" android:exported="false" />

        <!-- File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
